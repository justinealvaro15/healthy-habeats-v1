import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const IntakeWaterContainer = () => {
    return(
        <View>
            <Text>Container for water intake</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default IntakeWaterContainer;